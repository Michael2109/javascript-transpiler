{
  "name": "compiler",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "start": "npx tsc && node dist/app.js",
    "clean": "node scripts/clean.js",
    "build": "npm run clean && tsc",
    "install-compiler": "npx tsc && npm install -g .",
    "test": "npm run build && jest",
    "test:unit": "jest --testPathPattern=test/unit",
    "test:corpus": "jest --testPathPattern=test/corpus/corpus",
    "test:cli": "npm run build && jest --testPathPattern=test/corpus/cli",
    "test:update": "node scripts/update-goldens.js"
  },
  "bin": {
    "lumina": "dist/app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "@types/node": "^20.5.7",
    "jest": "^29.6.4",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@types/js-beautify": "^1.14.0",
    "js-beautify": "^1.14.9",
    "node-run-cmd": "^1.0.1",
    "ts-node": "^10.9.1"
  }
}
